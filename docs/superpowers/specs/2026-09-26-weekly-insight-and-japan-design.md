# US 주간 인사이트 + 일본 주간 — 설계 (2026-09-26)

사용자 지시(2026-09-26): 「일본 레포트를 따로 두고, 숫자판은 매일 발행하지 않고 주간 글만. US 주간은 단순 요약에서 벗어나 인사이트를 얻을 수 있게 재구성. 추천 뼈대로 진행하고 결과를 보고 수정.」

## 왜 바꿨나 — W38 실측

- 서사를 그 주 일간 5편에서만 뽑으니 일간이 놓친 것을 주간도 놓쳤다. W38 은 원·달러 +3.35%·엔·달러 +2.15% 를 표에만 싣고 본문에서 한 번도 설명하지 않았다(그 주 지수보다 큰 움직임).
- 작성 규칙에만 있고 게이트가 없는 것은 그대로 발행됐다: 「잠든 트리거 → 문턱이 빡빡하다」 단정(지시문 금지), 9/13 지시 뒤에도 요일순 문단.
- W38 의 「~죠」 8회는 루틴이 「~다 4연속」 게이트를 피하려고 일부러 넣은 것이다(루틴 로그 `cse_01WP4ttmzNZrfBvA4irFYTQz`). 지금은 `data-register="da"` 의무화로 막힌다.
- KR W38 미발행은 정상 동작 — 9/10-18 KR 일간이 없어 「원본 없음」으로 멈췄다.

## 네 안과 선택

질문 심화(A) · 가설 결산(B) · 크로스에셋 진단(C) · 다음 주 플레이북(D). **C 를 엔진, A 를 틀, D 를 끝 절**, B 는 표로 축소. B 를 척추로 삼지 않은 이유: 원장 6건.

## 스파이크 (2026-09-26, 한국 IP)

| 항목 | 결과 |
|---|---|
| `ZQ=F` | 9/25 에 9월물 96.2525 → 11월물 값 95.965 로 조용히 롤. **금지**, 월물 명시 |
| 월물 내재금리 9/25 | 10월 3.895 · 11월 4.035 · 12월 4.175 (EFFR 3.88) |
| CFTC TFF/legacy | 5개 계약 정상, 엔 1,058주. 일시 연결 리셋 1회 → 재시도로 해결 |
| MOF `week.csv` | cp932. Python 은 물결표를 ～(U+FF5E), iconv 는 〜(U+301C)로 디코드. 연말 주는 끝 날짜에 연도 |
| MOF JGB CSV | 토요일 새벽에는 목요일까지만 → 일본 주간은 일요일 |
| MOF 입찰 결과·일정 | 1차 제외(표 구조 복잡, 결과 URL 미확인) |
| `^TOPX` | 야후에 없음 → ETF 1306·1615·1343·1622 |
| 도쿄 CPI 공표일 | 총무성에서 확인 못 함 → 일정에서 뺌 |
| BOJ | 10/1 08:50 의견 요약 + 단칸(9월), 10/29-30 회의 — 공식 일정표 확인 |

## 구조

- **공용 주간 스냅샷** `collect_weekly_data.py` → `data/weekly_ext/<KEY>.json`. 매일 수집 없음 — 소스가 이력을 통째로 준다. 표 끝값은 기존 집계(발행본 종가) 그대로라 「끝값 = 마지막 발행본 종가」 불변식이 유지된다.
- **US**: `us/weekly_insight.py`(z = 주간 변화 ÷ 직전 52주 주간 표준편차, 26주 미만이면 매기지 않음; 커브 레짐; 주가-금리 20일 상관; 포지션 3년 백분위; 연준 선물 월물 경로; 다음 주 일정), `weekly_insight_gate.py`(check_period `--insight`). 작성자는 절 산문만, 표는 조립.
- **일본**: `scripts/japan/` — 코어 6·커브·흐름·헤지 근사·포지션·주식·고정 신호. 게이트는 부호·근사·기준일·두 갈래.
- W38 재현: 원·달러 z 2.3 → 「설명 필요」. 엔·달러 z 1.7 은 표가 금리차(+9.4bp)와 함께 보여 준다.

## 알려진 한계 (codex 검토 대상)

- 2년 금리차의 1주 기준값은 FRED(전주), 최신값은 발행본 종가(네이버) — 출처가 섞인다.
- 연준 선물 1주 변화는 수집일 기준 5거래일 전이라 주 경계와 정확히 맞지 않을 수 있다.
- 신호 임계값(5bp·10bp·1.5%·2%p)은 판단값이다. 채점이 쌓이면 다시 본다.
- 일본 뉴스는 US·KR 뉴스 수집의 `region: japan` 행뿐이다 — 9/26 이전 파일에는 거의 없다.
