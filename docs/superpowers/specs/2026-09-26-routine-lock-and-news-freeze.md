# 루틴 선점 잠금 · 뉴스 동결 · 브리프 선발행 · 뉴스 블록 확장 (2026-09-26)

## 사고

9/25 US 브리프와 W39 주간이 발행되지 않았다(루틴 로그 `cse_01RnWTAeTcBPRUrsbpz1G7ww`, `cse_01TFhE5rPWMmpVu9dcE9o5WA`).

- US 루틴이 9/25 19:10Z~9/26 01:08Z 에 10번 떴고, 00:10·00:24·00:50·01:08Z 넷이 동시에 작성했다(푸시 웹훅 두 벌 + 예약 크론).
- 09:10 KST 런은 브리프·뉴스 브리프가 게이트를 다 통과했는데, 발행 직전 `git pull` 에서 10:08 재수집 커밋을 받았다. 재수집이 `summary_ko` 를 새로 만들어 뉴스 게이트가 40건 어긋났고, 고치다 01:22Z 에 5시간 한도.
- 발행(STEP 3)이 뉴스 글(STEP 2.7) **뒤**에 있어 통과한 브리프도 함께 사라졌다.
- 12:17 KST 주간 루틴은 같은 한도 창에서 0초 사망.
- 뉴스·산업 브리프 작성자 한 번이 25만 토큰·20분(65 도구 호출) — 요약 26건을 다듬어 옮기다 게이트에 걸리는 반복.

## 조치

| # | 조치 | 파일 |
|---|---|---|
| A | 선점 잠금: `refs/heads/locks/us-<KST 날짜>` 를 부모 없는 빈 커밋으로 만든다. 한 번만 생기고, 150분 지난 잠금은 `--force-with-lease` 로 넘겨받는다. 쓰지 않고 끝나는 경로는 푼다 | `scripts/ci/run_lock.sh`, ORCHESTRATOR STEP 0-0 |
| B | 뉴스 동결: 요약된 기사가 있는 날은 다시 긁지 않는다(`--refresh` 로만). 갱신해도 같은 guid 는 요약 재사용. 요약기는 이미 요약된 기사를 부르지 않는다. 수집기가 「이미 완료」로 건너뛰면 뒤 단계·커밋도 건너뛴다 | `fetch_news.py`, `news_summary.py`, `collect_market_data.py`, 워크플로 |
| C | 주간 루틴 토 12:00 → 15:00 KST(US 일간과 다른 5시간 창) | 트리거 `trig_01FtzWEhguJSMKyKVTYUQBsA` |
| D1 | 브리프를 먼저 커밋·푸시하고 뉴스 글은 그 뒤(STEP 3.5), 자체 커밋 | ORCHESTRATOR |
| D2 | 뉴스 블록은 `<!--NEWS:guid|제목-->` 한 줄 → `expand_news_items.py` 가 요약 원문으로 채운다. 요약 문단(`data-summary`)은 문체 게이트의 어미 반복 검사에서 빼고, 뉴스 게이트가 원문 그대로인지 본다 | `us/news_expand.py`, `news_gate.py`, `style.py`, news-industry-writer |

## 남은 것

- 웹훅 자체는 이 도구로 조회·삭제가 안 된다(claude.ai 루틴의 GitHub 이벤트 트리거로 보인다). 잠금이 있어 병렬 작성은 막히지만, 뜰 때마다 짧은 런 비용은 남는다 — 루틴 화면에서 지우는 것이 근본 해결.
- 잠금 브랜치 push 도 웹훅을 부른다(뜬 런은 잠금에서 곧바로 끝난다).
- 요약 길이가 뉴스 게이트 분량(240~420자) 밖이면 원문을 넣어도 걸린다 — 그 기사는 직접 쓴다.
- codex 검토 빚(한도, 22:34 KST 해제).
